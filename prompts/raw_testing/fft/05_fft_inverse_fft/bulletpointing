"""
Generate a Python module to compute the Inverse Fast Fourier Transform (IFFT) 
in parallel using the PyCOMPSs framework.

Task Specifications:
- Functionality: Compute the inverse Fourier transform of a list of complex numbers 'x'. Assume a function `fft(x)` that computes the forward Fourier transform in-place is already available in the scope.
- Imports: Include all necessary PyCOMPSs imports (e.g., @task, compss_wait_on).
- Parallelization: Use one or more @task decorated functions to handle the computation (e.g., applying the conjugate, calling fft, and dividing by N).
- Orchestration: Provide a main(x) function that distributes the work and manages task dependencies.
- Synchronization: The main(x) function must explicitly wait for all tasks to complete.
- Return Type: Return a standard Python list of complex numbers (ensure it is the final synchronized result, not a list of futures).

Example:
- Input: [1.0+0j, 1.0+0j, 1.0+0j, 1.0+0j, 0.0+0j, 0.0+0j, 0.0+0j, 0.0+0j]
- Output: [0.5+0j, 0.125+0.301777j, 0.0+0j, 0.125+0.0517767j, 0.0+0j, 0.125-0.0517767j, 0.0+0j, 0.125-0.301777j]
"""