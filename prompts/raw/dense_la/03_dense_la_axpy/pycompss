"""
Generate a Python module to compute the AXPY (alpha*x + y) operation 
in parallel using the PyCOMPSs framework.

Task Specifications:
- Functionality: Compute the operation z = alpha*x + y, where 'x' and 'y' are vectors (lists of floats) of the same length, and 'alpha' is a scalar float.
- Imports: Include all necessary PyCOMPSs imports (e.g., @task, compss_wait_on).
- Parallelization: Use one or more @task decorated functions to handle the computation.
- Orchestration: Provide a main(alpha, x, y) function that distributes the work and manages task dependencies.
- Synchronization: The main(alpha, x, y) function must explicitly wait for all tasks to complete.
- Return Type: Return a standard Python list of floats representing the resulting vector 'z' (ensure it is the final synchronized result, not a list of futures).

Example:
- Input: alpha=2.0, x=[1.0, -5.0, 2.0, 9.0], y=[0.0, 4.0, 1.0, -1.0]
- Output: [2.0, -6.0, 5.0, 17.0]
"""