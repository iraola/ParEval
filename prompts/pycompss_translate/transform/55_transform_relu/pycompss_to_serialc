"""
Translate the provided parallel PyCOMPSs Python code into a standard, serial C implementation.

Task Specifications:
- Language Conversion: Translate Python logic into C. Map Python lists/NumPy arrays to C arrays, using `malloc`/`free` if dynamic memory allocation is required. Define appropriate data types for all variables.
- De-parallelization Strategy: Completely remove the task-based architecture. Convert orchestrated `@task` function calls into standard synchronous C function calls or inline loops.
- Functionality & Accuracy: The C version must preserve the exact logic of the original code and produce identical output.

PyCOMPSs Code to Translate:
from pycompss.api.task import task
from pycompss.api.parameter import IN
from pycompss.api.api import compss_wait_on


@task(value=IN, returns=float)
def task_relu(value):
    return value if value > 0 else 0

def relu(x):
    result = [task_relu(value) for value in x]
    result = compss_wait_on(result)
    return result

Example Expected Output Structure:
- Standard C code (including standard library headers like stdio.h, stdlib.h).
"""