"""
Generate a Python module to compute the convex hull of a set of 2D points 
in parallel using the PyCOMPSs framework.

Task Specifications:
- Functionality: Find the set of points that defines the smallest convex polygon containing all the points in the input list 'points'.
- Imports: Include all necessary PyCOMPSs imports (e.g., @task, compss_wait_on).
- Parallelization: Use one or more @task decorated functions to handle the computation.
- Orchestration: Provide a main(points) function that distributes the work and manages task dependencies.
- Synchronization: The main(points) function must explicitly wait for all tasks to complete.
- Return Type: Return a standard Python list of tuples of floats `(x, y)` representing the vertices of the convex hull (ensure it is the final synchronized result, not a list of futures).

Example:
- Input: [(0.0, 3.0), (1.0, 1.0), (2.0, 2.0), (4.0, 4.0), (0.0, 0.0), (1.0, 2.0), (3.0, 1.0), (3.0, 3.0)]
- Output: [(0.0, 3.0), (4.0, 4.0), (3.0, 1.0), (0.0, 0.0)]
"""