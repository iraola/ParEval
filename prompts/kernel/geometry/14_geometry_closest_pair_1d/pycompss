"""Return the distance between the closest two elements in the vector x.
   Use PyCOMPSs to compute in parallel.
   Provide a main(points) entrypoint that takes the input data and returns the result.
   Example: 

   input: [7, 3, 9, 12, 31, 1]
   output: 2
"""