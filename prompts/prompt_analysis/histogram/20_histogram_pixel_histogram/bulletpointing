"""
Generate a Python module to compute the histogram of a grayscale image 
in parallel using the PyCOMPSs framework.

Task Specifications:
- Functionality: Count the number of pixels with each grayscale intensity (0-255) in the input list 'image'.
- Imports: Include all necessary PyCOMPSs imports (e.g., @task, compss_wait_on).
- Parallelization: Use one or more @task decorated functions to handle the computation (e.g., splitting the image array into chunks, computing partial histograms, and merging the results).
- Orchestration: Provide a main(image) function that distributes the work and manages task dependencies.
- Synchronization: The main(image) function must explicitly wait for all tasks to complete.
- Return Type: Return a standard Python list of exactly 256 integers representing the counts for each intensity bin (ensure it is the final synchronized result, not a list of futures).

Example:
- Input: [2, 116, 201, 11, 92, 92, 201, 4, 2]
- Output: [0, 0, 2, 0, 1, 0, 0, 0, 0, 0, 0, 1, ..., 0] (A 256-element list where index 2 is 2, index 4 is 1, index 11 is 1, index 92 is 2, index 116 is 1, index 201 is 2, and all other indices are 0)
"""