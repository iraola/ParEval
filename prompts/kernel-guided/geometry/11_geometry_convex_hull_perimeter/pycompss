"""
Generate a Python module to compute the perimeter of a convex hull 
in parallel using the PyCOMPSs framework.

Task Specifications:
- Functionality: Find the smallest convex polygon (convex hull) that contains all the 2D points in the input list 'points' and compute its total perimeter. Assume points are given as tuples of two floats (x, y).
- Imports: Include all necessary PyCOMPSs imports (e.g., @task, compss_wait_on).
- Parallelization: Use one or more @task decorated functions to handle the computation.
- Orchestration: Provide a main(points) function that distributes the work and manages task dependencies.
- Synchronization: The main(points) function must explicitly wait for all tasks to complete.
- Return Type: Return a standard Python float representing the total perimeter (ensure it is the final synchronized result, not a future).
- Provide your complete solution in a single Python code block.

Example:
- Input: points=[(0.0, 3.0), (1.0, 1.0), (2.0, 2.0), (4.0, 4.0), (0.0, 0.0), (1.0, 2.0), (3.0, 1.0), (3.0, 3.0)]
- Output: 13.4477
"""