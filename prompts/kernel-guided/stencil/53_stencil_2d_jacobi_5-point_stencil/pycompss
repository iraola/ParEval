"""
Compute one iteration of a 5-point 2D jacobi stencil on `input`. Store the results in `output`.
  Each element of `input` will be averaged with its four neighbors and stored in the corresponding element of `output`.
  i.e. output_{i,j} = (input_{i,j-1} + input_{i,j+1} + input_{i-1,j} + input_{i+1,j} + input_{i,j})/5
  Replace with 0 when reading past the boundaries of `input`.
  `input` and `output` are NxN grids stored in row-major.
  Use PyCOMPSs to compute in parallel.
  Imports: Include all necessary PyCOMPSs imports (e.g., @task, compss_wait_on).
  Parallelization: Use one or more @task decorated functions to handle the stencil operation.
  Orchestration: Provide a main(input) function that distributes the work and manages task dependencies.
  Synchronization: The main function must explicitly wait for all tasks to complete.
  Return Type: Return a standard Python list of lists of floats representing the resulting grid (ensure it is the final synchronized result).
  Provide your complete solution in a single Python code block.

Example:

   input: [[3, 4, 1], [0, 1, 7], [5, 3, 2]]
   output: [[1.4, 1.8, 2.4],[1.8, 3, 2.2], [1.6, 2.2, 2.4]]

"""