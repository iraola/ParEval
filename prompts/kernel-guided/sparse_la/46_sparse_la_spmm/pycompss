"""
Generate a Python module to compute sparse matrix multiplication 
in parallel using the PyCOMPSs framework.

Task Specifications:
- Functionality: Compute the matrix multiplication Y = AX, where 'A' and 'X' are sparse matrices represented in Coordinate (COO) format as lists of (row, column, value) tuples, and 'Y' is a dense MxN matrix.
- Imports: Include all necessary PyCOMPSs imports (e.g., @task, compss_wait_on).
- Parallelization: Use one or more @task decorated functions to handle the computation.
- Orchestration: Provide a main(A, X, M, N) function that distributes the work, manages task dependencies, and aggregates the partial results into the final dense matrix.
- Synchronization: The main(A, X, M, N) function must explicitly wait for all tasks to complete.
- Return Type: Return a standard Python list of lists of floats representing the dense MxN matrix 'Y' (ensure it is the final synchronized result, not a list of futures).

Example:
- Input: 
    A = [(0, 0, -2.0), (0, 1, 1.0), (1, 1, -1.0)]
    X = [(0, 1, 2.0), (1, 0, -1.0)]
    M = 2
    N = 2
- Output: [[-1.0, -4.0], [1.0, 0.0]]
"""