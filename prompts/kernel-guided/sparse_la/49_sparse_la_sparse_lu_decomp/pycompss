"""
Factorize the sparse matrix A into A=LU where L is a lower triangular matrix and U is an upper triangular matrix.
  A is a sparse NxN matrix stored in COO format.
  Use PyCOMPSs to compute in parallel.
  Imports: Include all necessary PyCOMPSs imports (e.g., @task, compss_wait_on).
  Parallelization: Use one or more @task decorated functions to handle the computation.
  Orchestration: Provide a main(A, N) function that distributes the work and manages task dependencies.
  Synchronization: The main function must explicitly wait for all tasks to complete before returning the results.
  Return Type: Return a tuple (L, U) where 'L' and 'U' are lists of (row, column, value) tuples in COO format representing the resulting sparse matrices (ensure they are synchronized results).
  Provide your complete solution in a single Python code block.

Example:

   input: A=[(0, 0, 4), (0, 1, 3), (1, 0, 6), (1, 1, 3)]
   output: L=[(0, 0, 1),(1, 0, 1.5), (1, 1, 1)] U=[(0, 0, 4), (0, 1, 3), (1, 1, -1.5)]

"""