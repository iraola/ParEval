"""
Solve the sparse linear system Ax=b for x.
  A is a sparse NxN matrix in COO format. x and b are dense vectors with N elements.
  Use PyCOMPSs to compute in parallel.
  Imports: Include all necessary PyCOMPSs imports (e.g., @task, compss_wait_on).
  Parallelization: Use one or more @task decorated functions to handle the computation.
  Orchestration: Provide a main(A_coo, b) function that distributes the work and manages task dependencies.
  Synchronization: The main(A_coo, b) function must explicitly wait for all tasks to complete.
  Return Type: Return a standard Python list of floats representing the final solution vector x (ensure it is the final synchronized result, not a list of futures).
  Provide your complete solution in a single Python code block.

Example:
   
   input: A=[(0, 0, 1), (0, 1, 1), (1, 1, -2)] b=[1,4]
   output: x=[3,-2]

"""