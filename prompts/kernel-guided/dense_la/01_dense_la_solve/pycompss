"""
Solve the linear system Ax=b for x.
  A is an NxN matrix in row-major. x and b have N elements.
  Use PyCOMPSs to compute in parallel.
  Imports: Include all necessary PyCOMPSs imports (e.g., @task, compss_wait_on, and any parameter directions like INOUT if modifying state in-place).
  Parallelization: Use one or more @task decorated functions to handle the parallel computation steps.
  Orchestration: Provide a main(A, b) function that distributes the work and manages task dependencies.
  Synchronization: The main(A, b) function must explicitly wait for all tasks to complete.
  Return Type: Return a standard Python list of floats representing the solution vector 'x' (ensure it is the final synchronized result, not a list of futures).
  Provide your complete solution in a single Python code block.

Example:
   
   input: A=[[1,4,2], [1,2,3], [2,1,3]] b=[11, 11, 13]
   output: x=[3, 1, 2]

"""