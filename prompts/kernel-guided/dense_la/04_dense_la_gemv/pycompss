"""
Multiply the matrix A by the vector x. Store the results in the vector y.
  A is an MxN matrix stored in row-major, x has N elements, and y has M elements.
  Use PyCOMPSs to compute in parallel.
  Imports: Include all necessary PyCOMPSs imports (e.g., @task, compss_wait_on).
  Parallelization: Use one or more @task decorated functions to handle the computation.
  Orchestration: Provide a main(A, x) function that distributes the work and manages task dependencies.
  Synchronization: The main(A, x) function must explicitly wait for all tasks to complete.
  Return Type: Return a standard Python list of floats representing the resulting vector 'y' (ensure it is the final synchronized result, not a list of futures).
  Provide your complete solution in a single Python code block.

Example:

   input: A=[[1, -1, 2], [0, -3, 1]] x=[2, 1, 0]
   output: y=[1, -3]

"""